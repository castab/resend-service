# Values can be supplied directly in this type-safe HOCON file or through the
# optional environment substitutions below. HOST is loaded and logged but is
# not yet applied to the Jetty bind address.
port = 3000
port = ${?PORT}
host = "0.0.0.0"
host = ${?HOST}

databaseUrl = ${?DATABASE_URL}
resendApiKey = ${?RESEND_API_KEY}
webhookSecret = ${?RESEND_WEBHOOK_SECRET}
resendFrom = ${?RESEND_FROM}
resendReplyTo = ${?RESEND_REPLY_TO}
conversationApiKey = ${?CONVERSATION_API_KEY}
outboxDrainApiKey = ${?OUTBOX_DRAIN_API_KEY}
